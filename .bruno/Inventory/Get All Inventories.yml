info:
  name: Get All Inventories
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseUrl}}/api/inventories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
