info:
  name: Get All Stocks
  type: http
  seq: 1

http:
  method: GET
  url: "{{BaseUrl}}/api/stocks"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
