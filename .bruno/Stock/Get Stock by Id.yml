info:
  name: Get Stock by Id
  type: http
  seq: 2

http:
  method: GET
  url: "{{BaseUrl}}/api/stocks/:id"
  auth: inherit
  params:
    - name: id
      value: "{{stockId}}"
      type: path
      enabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
