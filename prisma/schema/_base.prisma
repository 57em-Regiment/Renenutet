generator client {
  provider             = "prisma-client"
  output               = "../../src/generated"
  moduleFormat         = "cjs"
}

datasource db {
  provider = "postgresql"
}

enum TransactionType {
  DEPOSIT
  WITHDRAW
  TRANSFER
  PRODUCTION
  LOSS
}
